# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Test database
DATABASE_URL="pgsql://postgres:password@brickOffersDb:5432/offers-app_test?serverVersion=15&charset=utf8"

# Email provider for tests
EMAIL_DEFAULT_PROVIDER=null
EMAIL_FROM=test@example.com
EMAIL_FROM_NAME='Test'

# AWS SES (not used in tests but required for container)
AWS_SES_REGION=us-east-1
AWS_SES_KEY=test-key
AWS_SES_SECRET=test-secret

# Other required env vars
GOOGLE_SEARCH_API_KEY=test
GOOGLE_SEARCH_ENGINE_ID=test
SEZNAM_SEARCH_API_KEY=test
BROWSERLESS_URL=http://localhost:3000
BROWSERLESS_TIMEOUT=30000
CHROME_SCREENSHOT_URL=http://localhost:3000
CLAUDE_API_KEY=test
CLAUDE_USE_CLI=false
CLAUDE_MODEL=claude-3-sonnet
CLAUDE_MAX_TOKENS=4096
